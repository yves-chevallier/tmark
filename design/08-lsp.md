# 08 — Language server and editor integration

Crate: `tmark-lsp`. Editor client: `editors/vscode`. Spec: §Tooling roadmap
item 6.

## Shape

`lsp-server` + `lsp-types` (synchronous, single main loop, a worker thread
for parse/resolve). Not `tower-lsp`: no async runtime in a process whose
work is CPU-bound and whose state is one map of open documents.

```
stdio ──▶ main loop ──▶ DocumentStore { uri → (text, Document, Resolved, diagnostics) }
                            │
                            ├── on change: parse (sync, < 50 ms) → publish parse diagnostics
                            ├── debounce 150 ms: resolve + lint on worker → publish all diagnostics
                            └── requests read the latest snapshot
```

Workspace: `tmark.toml` at the root (profile, lint levels, `include`
bases); a project is the set of `.md`/`.tm` files below it. Files not open
are loaded through `FsLoader` when resolution needs them (includes,
inventories) and cached by mtime.

## Features by milestone

| Feature | Source of truth | Milestone |
| ------- | --------------- | --------- |
| Diagnostics | parse → resolve → lint | 1 (parse), 2 (resolve, lint) |
| Formatting (document, range) | `tmark-fmt` with the workspace profile | 1 |
| Document symbols (outline) | headers, captions, containers, counter items | 1 |
| Folding ranges | containers, admonitions, fences, front matter | 1 |
| Semantic tokens | node kinds and *resolution state*: unknown role, unresolved reference, deprecated spelling. The TextMate grammar stays for instant colour; semantic tokens overlay what needs a registry. | 1 (kinds), 2 (resolution) |
| Completion | `@` → labels, bib keys, glossary terms with kind and preview; `{` → role names from `ROLES`; inside a role head → its keys; `:::` → container and admonition names; `{#` → nothing (definitions), `{.` → classes seen in the document; ```` ``` ```` → languages and node words; `{{` → front-matter paths; image paths → files under the document's directory (through `Loader::list`, added at milestone 3); front matter → keys and values from the JSON schema, merged with TeXSmith's `press` schema if a `tmark.toml` points to one | 2, 3 |
| Hover | reference → label word, number if known, caption text; role → spec excerpt from the registry doc comment; front-matter key → schema description | 2 |
| Go to definition / references | `Ref` ↔ host with `#id`, counter item, footnote, abbreviation; include → file | 2 |
| Rename | labels and counter keys, through `tmark-fmt::edit` | 2 |
| Code actions | apply a `Fix` (deprecated → canonical, sugar → canonical, add missing caption id) | 2 |
| Document links | includes, images, `[](other.md)` | 2 |
| Preview | Typst body compiled in-process (`typst` crate) to SVG pages, served to a webview; click → span → source (`typst-ide::jump_from_click` then the source map) | 4 |

## The VS Code extension

Today: the TextMate grammar generated by `scripts/build_grammar.py` from the
spec's recognisers, injected into Markdown, plus a `tmark` language id. At
milestone 3 the generator moves to Rust (`cargo run -p tmark-ir --example
grammar`) so that role names, node words and prefixes come from the
registries, and the extension gains an LSP client that starts the bundled
`tmark-lsp` binary (per-platform download like `ruff`/`tinymist`, or a WASM
fallback). The grammar stays: semantic tokens are an overlay, not a
replacement, because they arrive after the first paint.

File detection: `.tm`, `.tmd`, `.tmark` are TMark; `.md` is TMark when the
front matter has a `press` key, when the workspace has a `tmark.toml`, or
when the user sets `files.associations`. Otherwise the extension only
contributes the injected grammar and stays quiet (no diagnostics on a README).

## Protocol details worth fixing early

- Positions: the LSP speaks UTF-16 column offsets; `LineIndex` converts from
  byte offsets once, both ways. Test with emoji and combining characters.
- Diagnostics are published per file, including for included files that are
  not open (the workspace folder is the boundary).
- Every request is answered from the last complete snapshot; a request never
  waits for a parse.

## Implementation notes (milestone 3)

Decisions taken in `crates/tmark-lsp` that refine the text above.

- **Shape.** `run(connection)` in the library, `main.rs` only opens stdio,
  so the tests drive the server over `Connection::memory()`
  (`tests/protocol.rs`). The main loop `select!`s over the client's channel
  and the worker's result channel. Full text sync: a change replaces the
  text, the main loop parses (`tmark::parse_with` with the profile of the
  nearest `tmark.toml`) and publishes parse diagnostics with fixes attached;
  the worker (`worker.rs`) runs `tmark::analyse` 150 ms after the last
  change and returns `Resolved` plus the resolve and lint diagnostics. A
  result older than the document's version is dropped. Every request reads
  the store on the main loop; navigation and completion use the last
  analysis even when it is one version behind (a stale list beats none).
- **Measured.** The parser runs at about 510 ms/MB in release on this
  machine (spec ×14, 1 MB; the tokenizer alone is 500 of those), so a
  100 KB chapter costs 50 ms per keystroke on the main loop and a 1 MB file
  half a second. Acceptable for M3; the performance mandate of
  `13-handoff.md` (profiling, incremental sync) was not run.
- **Detection (ADR 0006).** `tmark` language id always; `markdown` when
  the front matter has a top-level `press:` line or a `tmark.toml` sits
  above the file. Undetected Markdown gets no diagnostics but answers
  structural requests. The client sends every Markdown document.
- **Positions.** `convert.rs`: `LineIndex` both ways, `file:` URIs to
  paths and back (percent-encoding, Windows drive letters). Included files
  are located through `Resolved.files`; their text is read from disk for
  rename edits (an unsaved editor buffer of an included file is not seen).
- **Sub-spans.** Completion ranges come from the text at the cursor (a key
  being typed is not a node). Definition, references, rename and hover use
  `RefItem.key_span`, `Label.id_span` and `CounterItem.key_span` (IR review
  C1–C3); a rename is a `WorkspaceEdit` of those tokens, never a reprint.
  Labels are looked up by id, not by node id: node ids restart in every
  included file.
- **Semantic tokens.** Six custom types mapped to the grammar's scopes in
  `package.json` (`semanticTokenScopes`): `reference`,
  `unresolvedReference`, `citation`, `label`, `unknownRole`, `deprecated`.
  One token per line; the server sends `workspace/semanticTokens/refresh`
  when an analysis lands.
- **Outline.** Headers nest by level and own their section; captions,
  figures, admonitions, divs, includes and counter items are symbols. A
  `::: aside` (`Para([Aside])`) and a generated image are not listed or
  folded yet (three lines in `outline.rs`, IR review §2–3).
- **Configuration.** `tmark::Config` from the nearest `tmark.toml`
  (profile, bibliography, `[lint]`, `[press] schema`); re-read for every
  open document on `workspace/didChangeWatchedFiles`; a broken file is
  reported once through `window/showMessage`. The `press` schema path is
  parsed but not yet merged into front-matter completion.
- **Not done.** Range formatting; diagnostics for included files; hover on
  front-matter keys; completion of `{.` classes, image paths and `{{`
  paths; the `press` schema merge; incremental text sync.
