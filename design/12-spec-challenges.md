# 12 — Challenges to the spec

Points where implementing draft 3 exposed an ambiguity, an inconsistency or a
gap. Each entry names the section, the problem, the resolution taken by the
design (so that code and fixtures can proceed), and who closes it: **spec**
(the wording must change) or **code** (the design decided; the spec is fine).
Resolving an entry means editing `spec/tmark.md` and its fixture, then
deleting the entry from this file.

| # | Section | Problem | Resolution taken | Closes |
| - | ------- | ------- | ---------------- | ------ |
| C1 | §Lexical grammar, attribute list | The value alternative `\S+` can swallow the closing `}` (`{width=60%}` matches, but `{a=b}c` and `{k=v}` at end of line are fine only by luck; `{k=a}b}` is ambiguous). | Values are `"[^"]*"` or `[^\s}]+`. | spec |
| C2 | §Lexical grammar, role head | Same `\S+` flaw for key values in role heads; positional `[^\s=}]+` is right. | Same fix. | spec |
| C3 | §Cite, `@doi:` | The bare-reference key grammar `[A-Za-z][\w:.-]*[A-Za-z0-9]` cannot contain `/`, yet `@doi:10.1002/andp.19053221004` is the canonical example. | A `doi:` key accepts `[^\s\[\]()]*` ending on a non-punctuation character. Trailing `.`/`,`/`;`/`:` stay out. | spec |
| C4 | §Roles | "an unknown name is literal text" versus the closed registry: nothing says whether `{qty}[…]` should raise a diagnostic. Silent literal text hides typos (`{asid}[…]`). | Literal text (spec) plus lint hint `role-unknown` when the head is followed by `[` or `(`. | code |
| C5 | §IndexEntry | `#[**term**]` as sugar for `main=true` conflicts with "brackets hold content parsed as Markdown": a bold term that is not a main entry becomes unrepresentable in sugar. | Sugar kept; the canonical form `{index main=true}[term]` disambiguates; the printer never emits the bold sugar. Documented as an accepted lossy sugar. | spec (say it is lossy) |
| C6 | §Two sigils vs §IndexEntry/§CounterItem | The sigil section presents `#[…]` and `#(…)` as *the* forms; the catalogue says the roles `{index}` and `{counter}` are canonical and the sigils sugar. The printer must pick one. | Roles are canonical (matches the catalogue and P6's "one spelling per mechanism"; sigils stay the finger-friendly sugar). Revisit if authors find printed roles noisy. | spec (state it once, in §Two sigils) |
| C8 | §Attributes (zero-width nodes) | "whitespace on both sides collapses to a single space, and disappears before punctuation" is a rendering rule but is stated in the syntax section; the IR must decide whether the `Space` nodes exist. | Spaces stay in the IR (round-trip); writers apply the collapse. | code |
| C9 | §Front matter | "unknown keys fail at parse time" conflicts with the `press` namespace being shared with site generators that own other keys, and with TeXSmith owning `template`, `callouts`, … that TMark does not know. | TMark validates only the keys it reads (`declare`, `sources`, `features`, and the metadata keys) and preserves the rest; TeXSmith validates its own. Unknown keys *inside* a TMark-owned group are errors. | spec |
| C10 | §Include | "A block include is the include role alone on its line" — inline `{include}(…)` in a paragraph is not defined (error? inline splice?). | Inline includes are a diagnostic `include-inline` and render as literal text. | spec |
| C11 | §Data directives | `mermaid code` restores the listing, but what does `mermaid raw` mean? `raw` requires a backend name, and `mermaid` is not one. | `raw` is valid only when the language is a backend name (`latex`, `typst`, `html`); otherwise `fence-unknown-node-word`. | spec |
| C12 | §Ref | `[](other.md)` "section number of another document's main heading" needs an inventory; nothing says which alias. | Resolved only when `sources.crossrefs` has an entry whose `source` matches the path; else `ref-unresolved`. | spec |
| C13 | §Math | "No space directly after the opening delimiter" for `$…$` is the PyMdownX rule; `$$` on one line with attributes (`$$x$$ {#eq:a}`) is not covered. | Allowed; the attribute list attaches to the display math like on its own line. | spec |
| C14 | Appendix PyMdownX | Critic markup "fires even inside code spans" is a PyMdownX behaviour; TMark says nothing fires in code. | TMark never fires in code; critic markup is milestone 5 and follows TMark's rule. | spec |
| C15 | §Conformance, class E table | `pymdownx.betterem`, `smartsymbols`, `emoji`, `magiclink` are listed as extensions in the standard set, but the catalogue never assigns an IR node to emoji or smart symbols beyond `Str`. Fine, but magic links (bare URLs) are class C and produce `Link`; say so in §Inline. | `Link` with `target = Url`, autolink flag for the printer. | spec |
| C16 | §Two sigils | `#{…}` withdrawn but TeXSmith today ships `#{prefix:key}` and Ruby-style `#{user.name}` in prose must stay literal; the deprecated form's guard is not stated. | Deprecated `#{prefix:key}` is recognised only when the prefix is declared; otherwise literal. | spec |
| C18 | §Caption | Nothing requires the caption kind to match the host kind: `Figure:` after a table captions the table silently. | Attach anyway (today); propose an error `caption-kind-mismatch`. Say whether `Figure:`/`Listing:` before their float are sugar like `Table:` before. | spec |
| C20 | Appendix Deprecations | The table does not say which rows emit `deprecated` (horizon `fmt`) and which are silent sugar (`indefinite`); four `fmt` rows (`/// latex`, `/// caption`, `{index}[…]{b}`, `[](gls:term)`) neither warn nor normalise. | Emit for every `fmt`-horizon row. | spec (state it), code |
| C21 | §Roles | An unknown key or an invalid positional on a known role is silent. | Propose a hint `role-unknown-key`. | spec |
| C22 | §Round-trip | Entities (`&nbsp;`) are decoded by the tokenizer and printed as the character (U+00A0). | The printer should keep entities as typed; list them next to the escapes. | spec |
| C23 | §Lexical grammar | Caption lines accept a full attribute list, not only `{#id}` (universal `lang=`/`media=`). | Accept the full list. | spec |
| C24 | §Registries (lookup) | `@a:b` with an undeclared head `a` is a bibliography key, so an anchor `[x]{#claim:one}` or `::: note {#nt:x}` cannot be referenced with `@`, while `{#claim-one}` can. Editors then show `ref-unresolved` on ids the author just wrote. | Kept as specified (the resolver follows the lookup rule). Either declare that colon ids on anchors need a declared prefix, or let labels resolve before the bibliography. | spec |
| C26 | §Lexical grammar, role arguments | A `(…)` argument allows balanced parentheses inside and has no escape: a `RawInline` (inline HTML lowers to one) whose text holds an unbalanced `)` or `(` cannot be printed (`<span title="x)">`). | Not representable today; either add `\(`/`\)` escapes to the argument grammar (parser decodes, printer encodes) or state the limitation. Printer critic U9. | spec |
| C27 | §Lexical grammar, bare reference | Bibliography keys generated by Zotero / Better BibTeX (`1RgTv`, `7HA7H`; 10 of the 83 citations of `examples/book`) start with a digit, which the bare key grammar `[A-Za-z][\w:.-]*[A-Za-z0-9]` excludes; the bracketed item grammar reaches them only through its "whole item is the key" fallback. | The deprecated `[^key]` / `^[k1,k2]` forms accept a digit-initial key (an all-digit label stays a footnote); the printer writes such a key bracketed (`@[1RgTv]`), never bare. Either widen the bare grammar to `[A-Za-z0-9]` with an all-digit exclusion, or state that digit-initial keys need `@[…]`. | spec |
| C28 | §Lexical grammar, data directive info string | The family-4 regex allows only bare `key=value` options after the node word, yet the snippet and MkDocs examples write ```` ```md {.snippet caption="…" width="60%"} ```` and ```` ```mermaid {width=80%} ````, which have no representation: `.snippet` was dropped and the value became `"\"60%\"}"`. | The info string may end with a family-1 attribute list in braces (C1 value grammar); its classes and id live in `CodeBlock.options` / the generated `Image.attrs`, and the printer keeps the braces when there are classes or an id (bare words otherwise). Fixture `fence-attributes`. Add `(?:\s+\{…\})?` to the regex. | spec |
| C30 | §Table rung 5 | The spec says "`~` acknowledging absorbed slots" and defers the rest to TeXSmith's validator, whose behaviour differs from its own documentation in four corners: a slot absorbed by a row span needs no `~` once a cell was placed before it in the row (`skip_absorbed`), yet a `~` written there is read as the next column's empty cell; the row label is `str()`-ed, so a rich or list first cell is garbage rather than an error; a scalar or list addressed to a partly consumed group is sized to the whole group and overruns it; in named-row mode an omitted column under a span counts as an extra cell. The spec also never states the row grammar (first item is the label column, one item per top-level column, a group's leaves as a list, column-span slots unwritten) or the two-column minimum. | The documented rule is the spec: every slot a row span absorbs is `~` (inside a group list too), the Python reading is accepted as a fallback so no accepted document changes meaning; the first column is a column like the others (a rich label spans right); an item fills its column's *remaining* leaves; an omitted named column under a span is left absorbed; at least two columns. Implemented in `tmark-syntax::lower::table_yaml`; state the row grammar in §Table. | spec (state the row grammar), TeXSmith (align `schema.py` or replace it with tmark's model) |
| C29 | §Counters, `scope` | `scope: chapter` on `fig`/`tbl`/`lst`/`eq`/`thm` describes the backend's reset rule; a medium that numbers a series itself with no chapters (the web, design 06 §Site-wide resolution) numbers it continuously across the site, so the same document prints `Figure 3.2` and shows `Figure 12`. Nothing says `scope` is advisory, nor that "shared across a multi-document build" applies to backend series. | `scope` is a hint to whoever numbers the series; under site-wide numbering every series is `document` and continuous, and the chain (`start`/`next_start`) covers every series. | spec (one sentence under `start`, `scope`) |
| C31 | §Containers (tabs) | `=== "Tab"` (PyMdownX `tabbed`) is in the class-E extension table but has no node: the title is literal and the indented body a `CodeBlock`. Print has no tabs. | Container pair `:::: tabs` / `::: tab {title=…}`, nodes `Div{name=tabs}` / `Div{name=tab}`; `=== "Title"` plus its indented body is class-E sugar kept indefinitely (Material renders it); paged writers render the tabs in sequence as titled blocks (`tsdiv`), which is what TeXSmith ships; `container-orphan` hint for a `tab` outside `tabs`. Written into §Tabs; fixture `container-tabs`. | spec (closed) |
| C32 | §Div | `::: multicolumn` and every `Div` a template renders through `tsdiv` are `container-unknown`; §Div names no registry and says nothing about what an unknown container renders. | The container names are a closed registry: admonition types, `aside`, `figure`, `tabs`, `tab`, and the layout containers `multicolumn {cols=}` and `div`; the `tsdiv` / `#ts-div` contract with attribute forwarding is stated once; an unknown name stays a class-D `container-unknown` whose content renders transparently; no mechanism to declare container names (a new kind is an admonition type). Written into §Div; fixture `container-multicolumn`. | spec (closed) |
| C33 | §Inline text | `LaTeX`, `TeX`, `XeLaTeX` are logos in TeXSmith (`TexLogo` node from a tree processor) and plain `Str` in tmark; a `{tex}` role, a feature, or nothing? | Nothing in the IR: the words stay `Str`; feature `typography.tex-logos` (on) makes the writers set a fixed word list as logos, form applied by the backend like language spacing (P1), never in code, math, raw, destinations or attribute values; no per-word opt-out. Written into §TeX logos and the feature table; no fixture (no node). | spec (closed) |
| C34 | Appendix PyMdownX, §Inline | `:smile:` is "`Str`" in the appendix without saying who expands it or from which table; `:material-…:` icons have no row and no print behaviour. | The tokenizer expands emoji shortcodes from GitHub's `gemoji` table into the character, which the printer emits (class E; unknown names literal, silent). Icon shortcodes of the four Material sets lower to `Span{.icon media=web}` holding the shortcode, so print drops them through the media rule and the HTML writer emits a hook span; hint `icon-web-only`; class D. Written into §Emoji and icon shortcodes; fixtures `inline-emoji`, `inline-icon`. | spec (closed) |
| C35 | Appendix PyMdownX, §Attributes | `ProgressBar` is named in the appendix but defined nowhere (fields, inline or block, canonical spelling); TeXSmith's documents write the Python-Markdown `{: .thin}` attribute colon, which no grammar accepts. | `ProgressBar` is an inline node `{value, label}` with the PyMdownX percentage spelling canonical, the fraction form deprecated (fmt); `{: …}` is accepted on every host as deprecated sugar (fmt), stated in §Attributes and the lexical grammar. Written into §ProgressBar; fixtures `progressbar`, `attributes-colon`. | spec (closed) |
| C36 | §HorizontalRule, X2, draft-1 table | "paged templates render `\clearpage`" names a LaTeX command the writer would have to emit, so a template could not restyle the divider without patching the writer. | The writers emit `\tsdivider` / `#ts-divider()`, defined by `ts-typesetting` as a page break and redefinable by the template; the HTML writer emits `<hr>`. Wording fixed in the three places; fixture `divider`. | spec (closed) |
| C37 | §Raw, §Div (closes C19) | HTML other than comments had no canonical spelling; `<div markdown>` (`md_in_html`, in the class-E set) swallowed its content as raw. | HTML is kept as typed (class C) as `RawInline`/`RawBlock{format=html}`; the printer prints HTML-shaped text as typed and other payloads as the raw role/fence; paged writers drop it. A block whose tag carries `markdown` is sugar for `::: <tag>`; `<div markdown>` is `::: div`, class E, kept indefinitely and emitted by the `mkdocs` profile. Written into §Raw and §Div; fixtures `html-raw`, `container-div-markdown`. | spec (closed) |
| C38 | §Header | Nothing says whether a heading without `{#id}` can be targeted by `[text](#slug)`, which GitHub, MkDocs and Pandoc all allow, nor which slug rule (python-slugify, TeXSmith's writer today, transliterates; Python-Markdown `toc` drops accents; GitHub keeps Unicode). | Implicit ids exist, derived at resolution with GitHub's rule (Unicode kept, `-1` suffixes), never stored or printed, replaced by an explicit id; a reference to one is the hint `ref-implicit-id`. Written into §Header; fixture `heading-implicit-id`; open question 12 for sites with another slugifier. | spec (closed) |
| C39 | §Header | No per-heading "unnumbered" marker; only TeXSmith's document-level `numbered`. | Pandoc's `.unnumbered` and `.unlisted` classes, per heading, over the `press.numbered` default; `{-}` stays literal (no bare-word attributes) and is the importer's job. Written into §Header; fixture `heading-unnumbered`. | spec (closed) |
| C40 | §Container directives | `::: texsmith.core.config` (mkdocstrings, no closing fence) collides with the container fence: `container-unknown` plus `container-unclosed`, and the rest of the file is swallowed. | A dotted name is a *foreign directive*: the line plus its indented continuation, closed by dedent, `RawBlock{format=markdown}`, printed verbatim, dropped by the other writers with the hint `directive-foreign`; recogniser added to the lexical grammar. Written into §Foreign directive; fixture `directive-foreign`. | spec (closed) |
| C41 | §Inline text, Appendix PyMdownX | `^^x^^` is "not a TMark construct" yet the appendix maps it to `{underline}[x]` under `inline.insert`; with the feature off nothing says what happens. | Confirmed: on, sugar for `{underline}[x]` (one `Underline` node, printer emits the role); off, literal text with the hint `feature-off`. Written into §Inline text; fixtures `inline-insert`, `inline-insert-off`. | spec (closed) |
| C42 | Appendix PyMdownX | `[TOC]` "accepted and ignored" cannot round-trip if ignored, and printed literally it reaches the PDF. | Same node as C40: `RawBlock{format=markdown}`, verbatim for the printer, nothing for the other writers, silent (class E) because the print equivalent is `press.toc`. Written into §Foreign directive; fixture `directive-foreign`. | spec (closed) |
| C43 | §Image, Figure | "Renders 'Figure 1' with '(a)', '(b)'; `@fig:crash` yields 'figure 1b'" describes the rendering and never says that a sub-figure takes no number of the `fig` series. Under site-wide numbering (design 06) each image of a container therefore consumed a figure number: a page with a plain figure and a `::: figure` of two labelled images read 1, 2, 3, 4 instead of 1 and 2 with 2a, 2b, and `next_start` was inflated by one per sub-figure. Nothing said what a lone image in a container, an unlabelled container or a container holding prose does either. | A sub-figure is a label with no number of its own (`Host::Subfigure`, `Label::subfigure`): the container advances `fig` once and each image resolves to the container's number suffixed with a letter in document order, lower case, wrapping after `z`. A lone image *is* the figure and shares its number with no letter; a container that is not a grid of image paragraphs is a plain float whose images number normally; a container with no anchor and no caption numbers nothing, nor do its sub-figures. The three writers and the web lowering letter exactly the list the registry lettered (`figure_images`, `subfigure_letter`). Written into §Image, Figure; fixture `container-figure-subfigures`, `crates/tmark-registry/tests/subfigures.rs`. | spec (closed) |
| C44 | §Para | The `paragraph.lead` sugar is defined as "a paragraph whose first inline is a strong span shorter than 80 characters", which fires on `**Leading bold:** followed by text`; but `\tslead` is `\par\noindent…\par\nobreak\smallskip`, so the promotion moves the rest of the sentence into its own paragraph, and a paragraph that *is* a bold label — the shape TeXSmith documents as "standalone bold paragraphs" and the only one the contract typesets correctly — was left a plain `\textbf` (parity finding F4, 11+ corpus entries). | The sugar promotes a paragraph whose *whole* content is one strong span under 80 characters, outside a list item; the `{lead}[…]` role keeps taking what follows it, whatever the feature says, and the lowering tells role from sugar by the source. Wording fixed in §Para and the feature table; fixtures `lead`, `lead-run-in`. Cost: fifteen CommonMark "Emphasis and strong emphasis" examples whose paragraph is one strong span now render `<strong class="lead">` and left the `PASSING` list of the suite. | spec (closed) |
| C45 | §Attributes, Appendix "Deprecation schedule" | `[](){#id}` — an empty link hugging an attribute list, the MkDocs/autorefs anchor idiom and the top of TeXSmith's `docs/about/release-notes.md` — has no row: the link is a link with an empty destination and the attribute list finds no host, so the output was `\url{}\{\#id\}` and every `\hyperref[id]` to it dangled (parity finding F5). | An empty link is no link: `[](){attrs}` lowers to the zero-width `Span` that `[]{attrs}` already produces, with `deprecated` carrying `[]{#id}` as its text fix. Written into §Attributes and the deprecation table; fixture `span-anchor-deprecated`. | spec (closed) |
| C46 | Appendix PyMdownX, §Quoted | The appendix maps `(c)`, `-->`, `1/2` to a `Str` and `"quotes"` to a `Quoted` without saying who applies them, from which table, or how far a quote pair may reach; nothing was applied at all, so `\enquote` and the symbols were lost in every document (parity finding F6). Two of the rows cannot be taken literally: `pymdownx.smartsymbols` also rewrites ordinal numbers (`1st`), which is a superscript rather than a `Str`, and SmartyPants pairs quotes across inline nodes, which the text-run scan cannot. | The eight `Str`-producing symbol groups of `pymdownx.smartsymbols` (with its boundaries) and TeXSmith's own double-quote pattern are applied in the text-run scan; ordinal numbers are not applied; a quote pair that straddles inline markup stays literal; single quotes are never paired; `--`/`...` stay as typed, since both backends typeset them. Written into the appendix rows; fixture `inline-smart-symbols`. Cost: fourteen CommonMark examples that carry a straight-quoted phrase left the suite's `PASSING` list. | spec (closed; revisit if a quote across inline markup shows up in a real document) |
| C47 | §Includes, Appendix PyMdownX | The snippet rows write the marker as `--8<-- "file"` and nothing says it is one spelling of `pymdownx.snippets`' `-{2,}8<-{2,}`, nor that a `;` before the marker disables the line. Only the two-dash form was recognised, and every occurrence in TeXSmith's corpus uses three (`---8<--- "file"`), so the include never fired and the line survived as literal text (parity finding F8). | Any `-{2,}8<-{2,}`, the two sides free to differ, is the deprecated sugar, in a paragraph and as a fence body alike; a leading `;` is PyMdownX's escape, silent, and the line is the text it spells less one `;`. The recogniser moved to `tmark_ir::registry` (`snippet_path`, `snippet_escape`), which the printer now shares: a paragraph escapes the literal marker with a backslash, a fence body — having no backslash escape — with the `;` itself, so both are fixed points. Written into §Includes and both appendix rows; fixtures `include-snippet-dashes`, `include-snippet-escaped`, `fence-include-snippet`. | spec (closed) |
| C48 | §HorizontalRule, X2, C36 | C36 made the divider a contract but kept one behaviour for every position: `\tsdivider` = `\clearpage`, `#ts-divider()` = `#pagebreak(weak: true)`. A `---` *inside a container* is then a page break inside a box — nonsense in LaTeX (the quote or callout is torn in two) and a hard error in Typst, which refuses it: "pagebreaks are not allowed inside of containers". `examples/markdown/features.md` has a `---` in a block quote, so `texsmith --format typst --build` failed on it outright. | One node, two contracts, chosen by the writer's own nesting. `ts-typesetting` gains `\tsrule` / `#ts-rule()`, a full-width rule with the surrounding skips that never breaks the page; the HTML writer emits `<hr class="rule">`. Each writer keeps a `container` depth (block quote, callout, figure, div, tab, list item, table cell, aside, footnote — anything below the document's top-level block sequence) bumped by a single `contained(|w| …)` wrapper at every recursion into contained blocks; depth 0 keeps emitting the divider. Written into §HorizontalRule, the X2 row and the draft-1 table; fixture `divider` carries both positions in all three backends. | spec (closed) |
| C49 | Appendix PyMdownX, §Comment, C14 | The appendix mapped the five critic spellings to `Underline`, `Strikeout`, `Highlight` and `Comment` — nodes an author already writes — so nothing in the IR said a mark was a *reviewer's* mark: a writer could not tell `{++x++}` from `{underline}[x]`, the `ts-critic` contract (`\tsins`, `\tsdel`, `\tssubst`, `\tscomment`, a `FRAGMENTS` row since the migration) was unreachable, and `{>>note<<}` normalising to a plain `Comment` contradicted §Comment's own next sentence, which says change tracking is visible content and "belongs to a separate node". The appendix also promised that no class-E sugar is emitted by the canonical printer, which cannot hold for a construct whose only spelling is the sugar. | The four annotations are a `Span` whose single class is `critic`, holding exactly the node the appendix names (`Underline`, `Strikeout`, the `Strikeout` and `Underline` of a substitution in order, `Comment`) — the separate node §Comment asks for, an attributed `Span` rather than a new node, and a shape a consumer that ignores the class still renders as the legacy renderer did. `{==x==}` is critic's spelling of `pymdownx.mark` and stays a plain `Highlight`: the contract defines no macro for it. The critic comment is typeset in print (`\tscomment`, `#ts-comment`) and zero width on the web. The printer emits the critic spelling in every profile: there is no other, and any invented one would drop from class E to a literal brace group on a foreign renderer. Recognised in lowering from the literal brace group the tokenizer hands over (design 02 rule of thumb), so C14 holds for free — nothing fires in code, math, raw or a destination — and the inner text is re-parsed as inline content. `compat-unsupported` stops reporting critic markup. Written into Appendix "Critic markup", the appendix intro and row, and §Comment; fixtures `critic-insert`, `critic-delete`, `critic-substitute`, `critic-highlight`, `critic-comment`; cross-repository (`Requires.fragments` gains `ts-critic`, `texsmith.typ` gains the four functions). | spec (closed) |
| C50 | §Glossary and acronyms, §Front matter | The section said only that "structured declaration (groups, per-group tables) lives under `declare.glossary`" and never gave the shape, so the key stayed a loose JSON value read as a flat term-to-definition mapping. The structured spelling TeXSmith documents (`docs/syntax/abbr.md`) and normalises (`passes/glossary.py`) was already in use: against it `style`, `groups` and `entries` were taken for three terms, the real terms under `entries` were declared nowhere, and `@gls:api` was `ref-unresolved` — `tmark check --strict` failed a document TeXSmith builds. | One typed declaration, `GlossaryDecl {style, groups, entries}`, reached by both spellings: the keys of the flat mapping become `entries`, the structured form is read as written, and the two may be mixed — the three structural keys are never terms at the top level, every other key there is one, and a term named after one of them is written under `entries` (where it wins over a flat key of the same name). `style` and `groups` are form: they stay in the front matter, for the template that prints the per-group tables, and never reach `Resolved.glossary`, which keeps holding the terms alone. A value that is neither a mapping nor a style string declares nothing rather than failing the front matter. Written into §Glossary and acronyms and design 06; fixtures `glossary-flat`, `glossary-structured`, `crates/tmark-registry/tests/resolve.rs`; cross-repository (the IR field changes type, so `texsmith/ir/model.py` and the schema hash are regenerated). | spec (closed) |
| C51 | §Cite, §Ref, Appendix "Deprecation schedule" | The section made `@key` the in-text (narrative) citation and `@[key, locator]` the parenthetical one and called it "Typst's one-`@`-for-all model". The LaTeX writer followed it literally (`\textcite` for a bare key, `\cite` for a bracketed one) and the Typst writer added `form: "prose"` to a bare key — but Typst's own `#cite(<key>)` renders the *short* form by default (`form: "normal"`), so the claim was not what the model does. Worse, the deprecated `[^key]` sugar (TeXSmith 0.6: `\cite{key}`, the short "[3]") was lowered as a bracketed `Ref` while its fix printed the bare `@key`: `tmark lint --fix` silently turned every migrated citation from "[3]" into "Wadhwani, McManus, and McMahon [3]" (confirmed in a built PDF). Two parts of the design already treated the forms as one: `Ref.bracketed` is a `SUGAR_FIELDS` entry (a spelling, not a meaning) and the canonical printer rewrote a lone `@[key]` as `@key`. The spec review (`design/reviews/08-spec-consistency.md`, B4) added two findings: §Ref ("brackets appear when there is a space") and the old §Cite sentence left `@[ein05]` undefined, and a document-level default alone would leave no per-citation narrative spelling, which Pandoc (`@key` versus `[@key]`) and Typst (`form:`) both have. | One rule: a bare `@key` is one key in the document's default form; `@[…]` is an explicit item list (prefix, flag, key, locator) and reads as written whatever the default. The default form of a bare key is the short citation the style gives, on every backend (`\cite{key}`, `#cite(<key>)`, the web's author-year link); a feature switch, `citations.narrative` (off, Table "The feature registry"; the writer option `citations.narrative` overrides the front matter, the precedence of `lang`), makes it the narrative one (`\textcite`, `form: "prose"`). Inside brackets a plain item is short and parenthetical, `+key` is narrative (`RefItem.narrative`, mirroring `-key` for the year alone), so both forms are always spellable: `@[+ein05]` in a short document, `@[ein05]` in a narrative one. `+` rather than another sigil because the item grammar already peels one flag character off the key and `+` is not a key character, so nothing becomes ambiguous; one flag per item (`+-key` is no key). A feature rather than a new `press` key: §Feature registry says every switchable behaviour is a row of that table and nothing else in the front matter toggles one, `typography.tex-logos` is the precedent for a switch the writers read, and the front-matter schema (hence TeXSmith's `model.py` and its schema hash) does not change. The `[^key]` / `^[k1,k2]` sugars lower as bare (they were the short form), so their fix is meaning-preserving under both settings; the canonical printer keeps a lone `@[key]` bracketed, since the brackets carry the parenthetical meaning under the switch (the field stays a sugar field: the IR comparison still ignores it). The HTML writer and the web lowering keep their one parenthetical author-year form and ignore the switch; the `mkdocs-bibtex` passthrough follows it, because Pandoc's bare `@key` is narrative (`[@key]` by default, `@key` under the switch). Written into §Cite, §Ref and the feature table; fixtures `reference-citation-deprecated` (given a bibliography, so its snapshots show `\cite`), `reference-citation-flags` and `reference-citation-narrative`; `crates/tmark-writers/tests/citations.rs`. Cross-repository: `RefItem.narrative` is a new IR field (`texsmith/ir/model.py` and the schema hash are regenerated), plus a new feature name and a new `write` option key; TeXSmith's `docs/syntax/references.md` and `docs/guide/features/bibliography.md` state the old rule. | spec (closed) |
| C17 | File extension | The spec assumes `.md`. Product discussions mention `.tm`. P3 (degradation on GitHub) argues for keeping `.md` primary. | `.md` primary; `.tm`/`.tmd`/`.tmark` accepted as explicit markers. ADR 0006. | spec (add a line) |

Open questions the spec already lists (Appendix "Open questions") are not
repeated here; their answers, when taken, go in the same appendix.

Closed: C19 (folded into C37); C49 (critic markup, written into the appendix and §Comment in the same commit; the row stays as the record of the decision); C31–C42 (the twelve open constructs of the migration audit, each written into the section its row names in the same commit; the rows stay as the record of the decision); C7 (attachment rule written into §Caption, including the `::: figure` case); C25 (quoted values gained `\"` and `\\` escapes in §Lexical grammar, fixture `attributes-value-quote`); C29 (`press.admonition_style`, TeXSmith's real key, added to the `press.callout_style` deprecation row; fixture `frontmatter-deprecated`).
