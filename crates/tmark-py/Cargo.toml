[package]
name = "tmark-py"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
# The facade with `fs`: the wheel ships the CLI's loader.
tmark = { path = "../tmark" }

[lib]
name = "_tmark"
crate-type = ["cdylib"]
