Grammars vendored from VS Code (MIT, © Microsoft) so that the tests can
tokenise TMark exactly as the editor does: `markdown.tmLanguage.json` comes
from `extensions/markdown-basics`, the `md-math*` files from
`extensions/markdown-math`. Refresh them from
https://github.com/microsoft/vscode when the editor's Markdown grammar changes.
